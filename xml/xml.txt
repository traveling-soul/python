1、XML和HTML都源自SGML
2、XML主要有3个元素：
	Schema(模式)：定义了XML文件的逻辑结构
	XSL(可扩展样式语言)：用于规定XML样式文档的语言
	XLL(可扩展链接语言，主要分为XLinks和XPointer两种)：进一步扩展了目前Web上已有的简单链接
2、在XML声明中可以使用3个属性，分别是版本、独立和编码。
其中独立属性用来指定正确显示一个文档时是否需要文档类型定义(DTD)